<?php


defined("ABSPATH") || exit;
/**
 * @var $router \WPWCore\Routing\Router
 */

if (getenv("LANDO")) {
    /**
     * Lando environment specific routes .
     * this is for testing purposes only.
     * these routes will work in LANDO instance only.
     */
}
